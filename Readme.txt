# Codebase to get the access token to get authenticated to GCP services #

1.  All the methods are tested and able to retrieve the token
2.  For local code development any 1 of them can be used
3.  Service account can be default in direct Google env space [ Not req if code is moved to Google ]
4.  There can be some changes to values such as URL, Location, Service account key path etc
5.  Method - 5 Change the GCP service URL as per your requirement

NOTE ->>>> Change the method call below to invoke each 

For More Info check the Google docs ->>>> https://cloud.google.com/docs/authentication/token-types
Further study links                 ->>>> https://google-auth.readthedocs.io/en/latest/user-guide.html#application-default-credentials
